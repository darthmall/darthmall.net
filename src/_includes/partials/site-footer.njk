<footer id="site-footer" role="contentinfo" class="pad-block-lg type-aside"
  data-columns="all">
  <hr />
  <p>{% copyright %}</p>

  <p>
    Colophon: <a href="http://www.velvetyne.fr/fonts/sporting-grotesque/">Sporting
      Grotesque</a> &amp; <a href="https://brailleinstitute.org/freefont">Atkinson
      Hyperlegible</a>. <a href="https://github.com/edent/SuperTinyIcons">SuperTiny</a>
    icons. Built with <a href="https://11ty.dev">Eleventy</a>.
  </p>

  <nav>
    <ul class="list-reset" data-layout="cluster">
      {% for link in social %}
        {% set relAttr = "" %}
        {% set titleAttr = "" %}
        {% set content = link.text %}

        {% if link.rel %}
          {% set relAttr = " rel='" + link.rel + "'" %}
        {% endif %}

        {% if link.icon %}
          {% set titleAttr = " title='" + link.text + "'" %}
          {% set content %}{% include "icons/" + link.icon %}{% endset %}
        {% endif %}

        <li>
          <a href="{{ link.url }}"{{ relAttr | safe }}{{ titleAttr | safe }}>
            {{ content | safe }}
          </a>
        </li>
      {% endfor %}
    </ul>
  </nav>
</footer>
