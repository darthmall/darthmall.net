---
classes: h-entry
---

{% extends 'layouts/base.njk' %}

{% block content %}
  <article class="flow margin-inline-auto h-entry">
    <header class="flow">
      <h1 id="Skip" tabindex="-1" class="p-name">{{ title }}</h1>
      <p>{% formatDate date, "dt-published" %}</p>
    </header>

    <div class="flow e-content">
      {{ content | safe }}
    </div>

    {% include "partials/webmentions.njk" %}
  </article>
{% endblock %}
